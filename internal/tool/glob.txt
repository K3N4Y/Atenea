Glob files and directories

<instructions>
- pattern: one ripgrep-style glob; use braces for alternatives, such as `*.{go,md}` or `{agentcore,internal}/**/*.go`.
- path: one workspace-relative directory to search; use separate calls for independent search roots.
</instructions>

<avoid>
Open-ended multi-round discovery 
</avoid>
